using System.Collections;
using UnityEngine;
using UnityEngine.UI;

public class Roll : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //testedPlayer = GameObject.Find("testedPlayer");
    }

    // Update is called once per frame
    void Update()
    {
    }
}
